<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Battleship</title>
</head>
<body>
<div>** {{ status }} **</div>
<br />
<br />
{{ board | raw }}
<div>
    <span>Enter coordinates (row, col), e.g. A5</span>
    {{ form_start(form)}}
    {{ form_widget(form)}}
    {{ form_end(form)}}
</div>
</body>
</html>